# Extra services that must spun up before this crate can be tested
services:
  redis:
    image: redis:8
    ports:
      - "56379:6379"
    options: >-
      --health-cmd="redis-cli ping || exit 1"
      --health-interval=5s
      --health-timeout=5s
      --health-retries=5
