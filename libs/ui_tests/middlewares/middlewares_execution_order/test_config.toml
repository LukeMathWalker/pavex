description = "Middlewares execute in the expected order"

[expectations]
codegen = "pass"

[dependencies]
tokio = "1"

[dev-dependencies]
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
