description = "pavex cannot resolve concrete generic parameters if they are not using an absolute path (yet)"

[expectations]
codegen = "fail"

[dependencies]
http = "0.2"
hyper = { version = "0.14", features = ["server", "http1", "http2"] }
