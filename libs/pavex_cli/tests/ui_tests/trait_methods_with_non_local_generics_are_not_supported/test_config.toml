description = "pavex does not yet support trait methods that rely on non-local generic parameters (e.g. generics defined on the trait itself)"

[expectations]
codegen = "fail"

[dependencies]
http = "0.2"
hyper = { version = "0.14", features = ["server", "http1", "http2"] }
