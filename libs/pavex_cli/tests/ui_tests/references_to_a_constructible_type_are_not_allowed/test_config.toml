description = "pavex does not know how to deal with shared references, even for types that have a registered constructor"

[expectations]
codegen = "fail"

[dependencies]
http = "0.2"
hyper = { version = "0.14", features = ["server", "http1", "http2"] }
