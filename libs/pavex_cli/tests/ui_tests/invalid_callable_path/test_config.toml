description = "Verify the quality of the compiler error when an invalid path is provided to f!"

[expectations]
codegen = "fail"

[dependencies]
http = "0.2"
hyper = { version = "0.14", features = ["server", "http1", "http2"] }
