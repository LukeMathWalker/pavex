description = "pavex supports pattern bindings for input parameters"

[expectations]
codegen = "pass"

[dependencies]
http = "0.2"
hyper = { version = "0.14", features = ["server", "http1", "http2"] }
