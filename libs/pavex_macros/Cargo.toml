[package]
name = "pavex_macros"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = "2.0"

[dev-dependencies]
trybuild = "1.0"
pavex = { path = "../pavex" }
serde = { version = "1.0", features = ["derive"] }
